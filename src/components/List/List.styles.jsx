import {List} from "@mui/material"
import {styled} from "@mui/system"


export const NestedListContainer = styled(List)(() => ({
        width: '100%',
        maxWidth: 360,
        bgcolor: 'background.paper'

    })
)

