// Core imports
import {BrowserRouter} from "react-router-dom";

// Local imports
import Router from "./routes";

function App() {
    return (
        <BrowserRouter>
            <Router/>
        </BrowserRouter>
    );
}

export default App;
